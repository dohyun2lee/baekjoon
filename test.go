package main

import (
	//"bufio"
	//"os"
	"fmt"
	//"reflect"
// 	"strconv"
// 	"sort"
 	"strings"
 )

func main() {
	//var mymap map[int]int
	//reader := bufio.NewReader(os.Stdin)
	//writer := bufio.NewWriter(os.Stdout)
	//defer writer.Flush()

	s := "abcde"
	S := strings.Split(s, "")

	fmt.Println(S[1:3])
}