package main

import (
	"container/list"
	"fmt"
)

var cnt int

func main() {
	var s string
	var word map[string]int = make(map[string]int)
	var 

	for i:=0;i<5;i++ {
		fmt.Scanln(&s)
		word[s]++
	}

	for k := range word {
		
	}
}
